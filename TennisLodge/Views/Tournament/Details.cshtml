@* @model TennisLodge.Web.ViewModels.Tournaments.TournamentDetailsViewModel

@{
	ViewData["Title"] = Model.Name;
}

<h2>@Model.Name</h2>

@if (!string.IsNullOrEmpty(Model.ImageUrl))
{
	<img src="@Model.ImageUrl" alt="@Model.Name" class="img-fluid mb-3" style="max-height: 300px;" />
}

<div>
	<p><strong>Description:</strong></p>
	<p>@Model.Description</p>

	<p><strong>Location:</strong> @Model.Location</p>
	<p><strong>Surface:</strong> @Model.Surface</p>
	<p><strong>Dates:</strong> @Model.StartDate - @Model.EndDate</p>
	<p><strong>Category:</strong> @Model.CategoryName</p>
	<p><strong>Organizer:</strong> @Model.Organizer</p>
</div>

<a asp-action="Index" class="btn btn-secondary mt-3">Back to list</a>
 *@